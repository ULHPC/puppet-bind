// --------------------------------------------------------------------------
//  /!\ DO NOT EDIT THIS FILE: it has been automatically generated by Puppet.
//  --------------------------------------------------------------------------
// prime the server with knowledge of the root servers
zone "." {
	type hint;
	file "<%= scope.lookupvar('bind::params::configdir') %>/db.root";
};

// be authoritative for the localhost forward and reverse zones, and for
// broadcast zones as per RFC 1912

zone "localhost" {
	type master;
	file "<%= scope.lookupvar('bind::params::configdir') %>/db.local";
};

zone "127.in-addr.arpa" {
	type master;
	file "<%= scope.lookupvar('bind::params::configdir') %>/db.127";
};

zone "0.in-addr.arpa" {
	type master;
	file "<%= scope.lookupvar('bind::params::configdir') %>/db.0";
};

zone "255.in-addr.arpa" {
	type master;
	file "<%= scope.lookupvar('bind::params::configdir') %>/db.255";
};


